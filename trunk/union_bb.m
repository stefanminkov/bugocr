function union_bb(boxes)
    done = false;
    while ~done
        % Find a collision
        for i=1:size(boxes,1)
            for j=1:size(boxes,1)
                if i==j
                    continue
                end
            end
        end
    end
end